<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%= @user.name %>'s Profile</title>
  </head>
  <body>
    <%= render 'user' unless @user.admin %>
    <%= render '/admin/admin' if @user.admin %>
    <%= link_to "Edit User Account", edit_admin_user_path(@user) if current_user.admin? && current_user != @user %>
    <br>
  </body>
</html>
