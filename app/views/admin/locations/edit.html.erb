<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <%= form_for @location do |f| %>
      <%= render 'locations/form', f: f %>
      <%= f.submit "Update Location" %>
    <% end %>
  </body>
</html>
