<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>All Users</title>
  </head>
  <body>
    <h2>Users</h2>

    <% @users.each do |user| %>
      <p><%= link_to "#{user.name}", admin_user_path(user)%></p>
    <% end %>
  </body>
</html>
