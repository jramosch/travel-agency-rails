<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Location Creator</title>
  </head>
  <body>
    <%= form_for @location do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit "Create Location" %>
    <% end %>
  </body>
</html>
